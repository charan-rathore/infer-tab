# Push

`git push` copies commits you already have locally to the remote named `origin` (or another remote).

You can only push what has been committed. Staged-but-uncommitted work, and unstaged edits, stay on your laptop.

The first push of a branch often needs `-u` so your local branch remembers which remote branch to update next time.
